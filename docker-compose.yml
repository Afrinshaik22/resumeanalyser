version: "3.9"

services:
  resume-analyzer:
    build: .
    container_name: resume-analyzer
    ports:
      - "5000:5000"  # host:container
    environment:
      - PORT=5000
    # Uncomment this for live-reload style development (not recommended for production)
    # volumes:
    #   - .:/app
